name: Validate JM TER collector

on:
  push:
    branches: [backend/jm-ter-v133]

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v6
      - uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d
        with:
          version: '0.11.33'
          python-version: '3.12'
          enable-cache: true
      - run: uv sync --frozen
      - run: uv run --frozen python -m compileall -q tracker scripts tests
      - run: uv run --frozen python -m unittest discover -s tests -v
      - name: Verify live JM Small Cap TER disclosure
        run: |
          uv run --frozen python - <<'PY'
          from datetime import date
          from tracker import amc_expenses,db
          db.init()
          day,plans,content_hash=amc_expenses._jm_ter_disclosure(date.today())
          assert set(plans)=={'Regular','Direct'}
          assert 0 <= plans['Direct']['base_expense_ratio'] <= plans['Direct']['ter'] <= 5
          assert 0 <= plans['Regular']['base_expense_ratio'] <= plans['Regular']['ter'] <= 5
          print('JM_VERIFIED',day,plans)
          print('JM_SOURCE',amc_expenses.JM_TER_API)
          print('JM_HASH',content_hash)
          PY
